version: '3'
services:
  monitor:
    image: image2pdf
    privileged: true
    ports:
      - 5000:5000
    volumes:
      - /fastdir:/fastdir
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure